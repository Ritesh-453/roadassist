# MySQL Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/my_app_db
spring.datasource.username=root
spring.datasource.password=Ritesh2580
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

# Server Port
server.port=8082
```

This will fix local. But wait — **if you push this to GitHub your password will be exposed again!**

So the proper fix is to add this to your `.gitignore`:
```
src/main/resources/application.properties